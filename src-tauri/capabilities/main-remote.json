{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-remote",
  "description": "Grant the remote WhatsApp Web page IPC access to whatRust's own commands (notify/set_unread). core:default is intentionally NOT granted so the remote origin cannot emit/listen on internal event channels or introspect the app.",
  "windows": ["wa-*"],
  "remote": {
    "urls": ["https://web.whatsapp.com/*"]
  },
  "permissions": ["allow-whatsapp-bridge"]
}
